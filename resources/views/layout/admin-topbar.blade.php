<header class="top-bar">
    <div class="title">
        <h1>{{ $title ?? 'Admin' }}</h1>
        <p>{{ $subtitle ?? 'Dany-Mart' }}</p>
    </div>
    <div class="user-info">{{ $user ?? 'Dimsum (Admin)' }}</div>
</header>
